---
id: none
title: No playbook
summary: Raw prompt only — sync no skills
---

(No extra instructions. Use the user prompt as-is.)

## Synced skills this playbook expects

Before launching the Cloud Agent, run:

```bash
./scripts/sync-skills-to-repo.sh /path/to/target-repo --playbook none
```

Then commit `.cursor/` in the target repo.

_None_
